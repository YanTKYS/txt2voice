# v0.9.3 リリースノート

## 配布検証・ライセンス整備

### バージョン番号修正

- `TxtToVoice/TxtToVoice.csproj` の `<Version>` を `0.9.1` → `0.9.3` に更新。
  v0.9.2 / v0.9.3 で未更新だったため、ヘルプ → バージョン情報 に正しい番号が表示されていなかった。

### THIRD_PARTY_LICENSES.txt 補完

- **NAudio** — "MIT License（jtalkdll と同様）" という参照形式から、MIT ライセンス全文（著作権表示付き）に変更。
- **System.Speech** — 著作権者を `.NET Foundation and Contributors` に明記し、MIT ライセンス全文を追記。

### release-checklist.md 整備

既存の必須チェック項目に加え、以下を追加・整理:

- **バージョン一致確認**: `<Version>` がリリースタグと一致しているかの明示的なチェック項目を追加。
- **THIRD_PARTY_LICENSES.txt チェック**: 著作権表示・ライセンス全文・CC BY 3.0 義務の確認項目を独立したセクションに整理。
- **配布 ZIP 内容確認**: ZIP 展開後の必須ファイル一覧（`TxtToVoice.exe` / `sample_dictionary.json` / `text_rules.json` / `THIRD_PARTY_LICENSES.txt` / `setup_openjtalk.ps1`）を明記。
- **setup_openjtalk.ps1 動作確認**: `-VerifyOnly` フラグの確認手順と期待結果を追加。
- **クリーン環境検証**: 別フォルダへの展開・初回起動・設定なし起動・ダイアログ表示確認を追加。
- Windows 依存テスト（RequiresEngine 除外）のコマンドを明記。
