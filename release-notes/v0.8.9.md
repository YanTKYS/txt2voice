# v0.8.9 リリースノート

## 新機能

### #131 テンプレートお気に入り固定（ピン留め）
- テンプレート一覧に ★ 列を追加。クリックでピン留めをトグル（☆ → ★）
- ピン留め済みテンプレートは選択中のソート順に関わらず常に先頭グループに表示
  - ピン留めグループ内では選択したソート順（最近使った順／タイトル順／使用回数順）が適用される
- ピン留め行は薄い黄色背景（`#FFFDE7`）で視覚的に区別
- `IsPinned` フィールドを `templates.json` に永続保存（既存ファイルは `isPinned: false` として扱われ後方互換）
